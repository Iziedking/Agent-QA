{
  "name": "agent-qa-memory-svc",
  "version": "0.1.0",
  "private": true,
  "description": "Portable agent memory sidecar: encrypts each user's items under their passphrase and keeps them on Walrus through MemWal, organised as user -> folder -> items, so any agent recalls the same memory from any machine.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@0gfoundation/0g-storage-ts-sdk": "^1.2.10",
    "@mysten-incubation/memwal": "^0.0.7",
    "@mysten/walrus": "^1.1.7",
    "ethers": "6.13.1"
  }
}
