{
  "name": "agent-memory-connect",
  "version": "0.1.1",
  "description": "Connect any MCP agent to Portable Agent Memory. Set up once with a blind-typed passphrase stored in the OS keychain; agents then talk to a local proxy that attaches the identity headers, so no secret ever sits in a config file.",
  "type": "module",
  "bin": {
    "agent-memory": "./bin.mjs"
  },
  "files": [
    "bin.mjs",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "mcp",
    "memory",
    "agent",
    "walrus",
    "model-context-protocol"
  ],
  "author": "Israel Dominic",
  "license": "MIT",
  "homepage": "https://agentsqa.xyz",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Iziedking/Agent-QA.git",
    "directory": "connector"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@napi-rs/keyring": "^1.3.0"
  }
}
