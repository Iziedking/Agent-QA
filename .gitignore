# ---- Internal planning & discussion (never commit) ----
PLAN.md
Guide.md
GUIDE.md
NOTES.md
notes/
scratch/
*.internal.md

# ---- Internal runbook and runtime data (never commit) ----
# The Docker and Caddy config files ARE tracked; they are project infrastructure.
# Only the internal step-by-step runbook and Caddy's generated data stay out.
deploy.md
caddy_data/
caddy_config/

# ---- Node (memory sidecar) ----
# node_modules are rebuilt from the lockfiles, which ARE tracked.
node_modules/
npm-debug.log*

# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
wheels/
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/
.python-version

# Test & coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.cache/
.mypy_cache/
.ruff_cache/

# ---- Environment & secrets ----
.env
.env.*
*.local

# Local MCP client config carries the user's identity; keep it out of the repo.
.mcp.json

# Internal runbooks (ours, not the product's). The public docs live alongside.
docs/demo.md
docs/walrus_feedback.md
deploy.md

# ---- Editors / OS ----
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
